import React from 'react';

const SideInfo = ({text}) => {

        return (
            <div className="side" >
               X <p>{text}</p>
            </div>
        );
}


export default SideInfo;